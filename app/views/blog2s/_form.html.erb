<%= form_with(model: blog2, local: true) do |form| %>
  <% if blog2.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(blog2.errors.count, "error") %> prohibited this blog2 from being saved:</h2>

      <ul>
      <% blog2.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title %>
  </div>

  <div class="field">
    <%= form.label :text %>
    <%= form.text_field :text %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
